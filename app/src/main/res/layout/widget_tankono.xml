<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="8dp"
    android:background="@color/ono_yellow">

    <!-- HLAVIČKA – pevná výška 56dp, bez RelativeLayout triků -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="vertical">

        <!-- Horní řádek: logo_text vlevo, datum vpravo -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <ImageView
                android:id="@+id/header_logo_text"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:layout_marginStart="4dp"
                android:adjustViewBounds="true"
                android:src="@drawable/logo_text"
                android:contentDescription="@string/app_name"/>

            <TextView
                android:id="@+id/header_datetime"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="end"
                android:layout_marginEnd="4dp"
                android:textColor="@color/ono_red"
                android:textSize="11sp"
                android:textStyle="bold"
                android:singleLine="true"/>
        </LinearLayout>

        <!-- Spodní řádek: logo_linka přes celou šířku -->
        <ImageView
            android:id="@+id/header_linka"
            android:layout_width="match_parent"
            android:layout_height="8dp"
            android:scaleType="fitXY"
            android:src="@drawable/logo_linka"
            android:contentDescription="@null"/>
    </LinearLayout>

    <!-- Text produktů – zbytek výšky -->
    <TextView
        android:id="@+id/rows_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:textColor="@color/ono_red"
        android:textSize="14sp"/>
</LinearLayout>